export type Folder = {
    id: string;
    name: string;
};

export type Page = {
    id: string;
    name: string;
    url: string;
    folderId: string;
};

export type Template = {
    id: string;
    name: string;
};
